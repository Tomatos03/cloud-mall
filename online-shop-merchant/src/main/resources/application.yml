spring:
  application:
    name: online-merchant
  profiles:
    active: dev

server:
  port: 7002

auth:
  white-list:
    - /merchant/auth/**
    - /merchant/files/**
    - /api/docs/**
    - /swagger-ui/**
