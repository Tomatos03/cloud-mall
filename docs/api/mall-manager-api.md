# 在线商城管理端接口文档

## 公共约束
### 响应结构
所有接口的响应遵循统一的数据结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

其中：
- `code`：状态码，0 表示成功，其他值表示错误
- `message`：响应消息
- `data`：响应数据，可能为对象、数组、分页结果等

分页响应结构中 `data` 包含以下字段：
- `records`：当前页记录列表
- `total`：总记录数
- `size`：每页数量
- `current`：当前页码
- `pages`：总页数

### 认证方式
默认所有接口均需要鉴权，采用 JWT Token 认证方式。仅在明确标注"无需鉴权"的接口可不提供 Token。

---

## 认证模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/auth`

### 1. 管理员登录  
#### 基本信息
**接口描述**：管理员通过用户名和密码登录系统  
**请求方式**：POST  
**请求路径**：`/login`  
**鉴权要求**：无需鉴权      

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |

**请求数据示例**

```json
{
  "username": "admin",
  "password": "123456"
}
```

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "tokenType": "Bearer",
    "expiresIn": 3600
  }
}
```

**响应字段说明**

| 字段名 | 类型 | 说明 |
|-------|------|------|
| accessToken | string | 访问令牌 |
| tokenType | string | 令牌类型，固定为 Bearer |
| expiresIn | integer | 令牌过期时间（秒） |

---

## 商品管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/goods`

### 1. 获取分类树
#### 基本信息
**接口描述**：获取商品分类树，包含级联关系
**请求方式**：GET
**请求路径**：`/category/tree`

#### 请求说明
**请求参数**：无

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "电子产品",
      "level": 1,
      "parentId": 0,
      "children": [
        {
          "id": 10,
          "name": "手机",
          "level": 2,
          "parentId": 1,
          "children": []
        }
      ]
    }
  ]
}
```

---

### 2. 获取商品单位列表
#### 基本信息
**接口描述**：获取所有商品单位列表
**请求方式**：GET
**请求路径**：`/units`

#### 请求说明
**请求参数**：无

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "件",
      "abbreviation": "pcs",
      "status": true
    }
  ]
}
```

---

### 3. 分页查询商品
#### 基本信息
**接口描述**：分页查询商品列表
**请求方式**：GET
**请求路径**：`/`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 是 | 页码 |
| pageSize | integer | 是 | 每页数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "苹果手机",
        "price": "3999.99",
        "stock": 100,
        "status": true
      }
    ],
    "total": 50,
    "size": 10,
    "current": 1,
    "pages": 5
  }
}
```

---

### 4. 获取商品详情
#### 基本信息
**接口描述**：根据商品ID获取商品信息
**请求方式**：GET
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 商品ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "name": "苹果手机",
    "categoryId": 10,
    "price": "3999.99",
    "stock": 100,
    "description": "最新款苹果手机",
    "status": true
  }
}
```

---

### 5. 获取商品详细信息
#### 基本信息
**接口描述**：获取商品的详细信息，包含全部SKU、规格、描述等
**请求方式**：GET
**请求路径**：`/detail/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 商品ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "name": "苹果手机",
    "skus": [
      {
        "id": 100,
        "specs": "128GB",
        "price": "3999.99"
      }
    ]
  }
}
```

---

### 6. 添加商品
#### 基本信息
**接口描述**：添加新商品
**请求方式**：POST
**请求路径**：`/`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| name | string | 是 | 商品名称 |
| categoryId | integer | 是 | 分类ID |
| price | string | 是 | 商品价格 |
| stock | integer | 是 | 库存数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 7. 修改商品
#### 基本信息
**接口描述**：修改商品信息
**请求方式**：PUT
**请求路径**：`/`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 商品ID |
| name | string | 是 | 商品名称 |
| price | string | 是 | 商品价格 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 8. 删除商品
#### 基本信息
**接口描述**：删除商品
**请求方式**：DELETE
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 商品ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

### 9. 修改商品上下架状态
#### 基本信息
**接口描述**：修改商品的上下架状态
**请求方式**：PUT
**请求路径**：`/{id}/status`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 商品ID |

**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| status | boolean | 是 | 上下架状态，true=上架，false=下架 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

## 订单管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/order`

### 1. 获取订单详情
#### 基本信息
**接口描述**：根据订单ID获取订单详情
**请求方式**：GET
**请求路径**：`/{orderId}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| orderId | integer | 是 | 订单ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "orderNo": "20250101000001",
    "userId": 10,
    "totalPrice": "9999.99",
    "status": "PAID"
  }
}
```

---

### 2. 分页查询订单
#### 基本信息
**接口描述**：分页查询订单列表
**请求方式**：GET
**请求路径**：`/page`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 是 | 页码 |
| pageSize | integer | 是 | 每页数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "orderNo": "20250101000001",
        "totalPrice": "9999.99",
        "status": "PAID"
      }
    ],
    "total": 100,
    "size": 10,
    "current": 1,
    "pages": 10
  }
}
```

---

### 3. 获取订单详细信息
#### 基本信息
**接口描述**：根据订单编号获取订单详细信息和订单项目列表
**请求方式**：GET
**请求路径**：`/detail/{orderNo}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| orderNo | string | 是 | 订单编号 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "orderNo": "20250101000001",
    "totalPrice": "9999.99",
    "items": [
      {
        "goodsId": 1,
        "goodsName": "苹果手机",
        "price": "3999.99",
        "quantity": 1
      }
    ]
  }
}
```

---

### 4. 取消订单
#### 基本信息
**接口描述**：取消订单
**请求方式**：POST
**请求路径**：`/cancel`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| orderId | integer | 是 | 订单ID |
| reason | string | 否 | 取消原因 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

### 5. 订单发货
#### 基本信息
**接口描述**：将订单状态从已支付改为已发货
**请求方式**：POST
**请求路径**：`/ship/{orderNo}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| orderNo | string | 是 | 订单编号 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 6. 获取订单评价
#### 基本信息
**接口描述**：获取订单的评价信息
**请求方式**：GET
**请求路径**：`/{orderNo}/comment`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| orderNo | string | 是 | 订单编号 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "orderNo": "20250101000001",
    "comment": "商品质量很好，发货速度也快",
    "rating": 5
  }
}
```

---

## 分类管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/category`

### 1. 获取所有分类树
#### 基本信息
**接口描述**：获取所有分类树，包含禁用分类
**请求方式**：GET
**请求路径**：`/allTree`

#### 请求说明
**请求参数**：无

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "电子产品",
      "level": 1,
      "parentId": 0,
      "status": true,
      "children": []
    }
  ]
}
```

---

### 2. 获取分类列表
#### 基本信息
**接口描述**：获取启用的分类列表
**请求方式**：GET
**请求路径**：`/list`

#### 请求说明
**请求参数**：无

#### 响应说明
**响应数据示例**：同获取所有分类树的响应结构

---

### 3. 获取分类树
#### 基本信息
**接口描述**：获取分类树
**请求方式**：GET
**请求路径**：`/tree`

#### 请求说明
**请求参数**：无

#### 响应说明
**响应数据示例**：同获取所有分类树的响应结构

---

### 4. 添加分类
#### 基本信息
**接口描述**：添加新分类
**请求方式**：POST
**请求路径**：`/add`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| name | string | 是 | 分类名称 |
| parentId | integer | 是 | 父分类ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 5. 更新分类
#### 基本信息
**接口描述**：更新分类信息
**请求方式**：POST
**请求路径**：`/update/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 分类ID |

**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| name | string | 是 | 分类名称 |
| parentId | integer | 是 | 父分类ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 6. 删除分类
#### 基本信息
**接口描述**：删除分类
**请求方式**：POST
**请求路径**：`/delete/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 分类ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 7. 更新分类状态
#### 基本信息
**接口描述**：更新分类的启用/禁用状态
**请求方式**：POST
**请求路径**：`/update/status/{id}/{status}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 分类ID |
| status | boolean | 是 | 分类状态，true=启用，false=禁用 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

## 轮播图管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/banner`

### 1. 创建或更新轮播图

#### 基本信息

**接口描述**：创建或更新轮播图
**请求方式**：POST
**请求路径**：`/`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 否 | 轮播图ID，不提供则新增 |
| title | string | 是 | 轮播图标题 |
| imageUrl | string | 是 | 图片URL |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 2. 批量删除轮播图
#### 基本信息
**接口描述**：批量删除轮播图
**请求方式**：DELETE
**请求路径**：`/batch/del`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| ids | integer[] | 是 | 轮播图ID列表 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 3. 分页查询轮播图
#### 基本信息
**接口描述**：分页查询轮播图列表
**请求方式**：GET
**请求路径**：`/page`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 是 | 页码 |
| pageSize | integer | 是 | 每页数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "title": "新年促销",
        "imageUrl": "https://example.com/banner.jpg",
        "status": true
      }
    ],
    "total": 10,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

---

### 4. 切换轮播图推荐状态
#### 基本信息
**接口描述**：切换轮播图的推荐状态
**请求方式**：POST
**请求路径**：`/recommend/{id}/{status}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 轮播图ID |
| status | boolean | 是 | 推荐状态 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

## 审核管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/audit`

### 1. 分页查询审核记录
#### 基本信息
**接口描述**：分页查询审核记录
**请求方式**：GET
**请求路径**：`/page`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 是 | 页码 |
| pageSize | integer | 是 | 每页数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "auditNo": "AUDIT20250101000001",
        "type": "GOODS",
        "status": "PENDING"
      }
    ],
    "total": 50,
    "size": 10,
    "current": 1,
    "pages": 5
  }
}
```

---

### 2. 获取审核详情
#### 基本信息
**接口描述**：按审核批次编号查询审核项目列表
**请求方式**：GET
**请求路径**：`/{auditNo}/detail`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| auditNo | string | 是 | 审核批次编号 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "auditNo": "AUDIT20250101000001",
      "itemId": 100,
      "reason": "商品描述不符合规范",
      "status": "PENDING"
    }
  ]
}
```

---

### 3. 获取审核记录详情
#### 基本信息
**接口描述**：根据审核记录ID获取审核详情
**请求方式**：GET
**请求路径**：`/{auditId}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| auditId | integer | 是 | 审核记录ID |

#### 响应说明
**响应数据示例**：同获取审核详情的响应结构

---

### 4. 审核决策
#### 基本信息
**接口描述**：对审核进行决策（通过或拒绝）
**请求方式**：POST
**请求路径**：`/decision/{type}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| type | string | 是 | 审核业务类型 |

**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| auditNo | string | 是 | 审核批次编号 |
| decisions | array | 是 | 审核决策列表 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

## 文件管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/files`

### 1. 上传文件
#### 基本信息
**接口描述**：上传文件到服务器
**请求方式**：POST
**请求路径**：`/upload`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | multipart/form-data |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| file | file | 是 | 要上传的文件 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "filename": "image.jpg",
    "fileUrl": "https://example.com/files/image.jpg",
    "fileSize": 102400
  }
}
```

---

## 秒杀管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/seckill`

### 1. 创建秒杀活动
#### 基本信息
**接口描述**：创建新的秒杀活动
**请求方式**：POST
**请求路径**：`/activities`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| name | string | 是 | 活动名称 |
| startTime | string | 是 | 开始时间 |
| endTime | string | 是 | 结束时间 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "name": "新年秒杀"
  }
}
```

---

### 2. 获取秒杀活动列表
#### 基本信息
**接口描述**：获取秒杀活动列表
**请求方式**：GET
**请求路径**：`/activities/list`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 是 | 页码 |
| pageSize | integer | 是 | 每页数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "新年秒杀"
      }
    ],
    "total": 20,
    "size": 10,
    "current": 1,
    "pages": 2
  }
}
```

---

### 3. 获取秒杀活动详情
#### 基本信息
**接口描述**：获取秒杀活动的详细信息
**请求方式**：GET
**请求路径**：`/activities/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 活动ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "name": "新年秒杀"
  }
}
```

---

### 4. 获取活动中的已通过商品
#### 基本信息
**接口描述**：获取活动中审核通过的商品列表
**请求方式**：GET
**请求路径**：`/activities/{id}/goods`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 活动ID |

**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 是 | 页码 |
| pageSize | integer | 是 | 每页数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "goodsId": 1,
        "goodsName": "苹果手机",
        "seckillPrice": "2999.99"
      }
    ],
    "total": 20,
    "size": 10,
    "current": 1,
    "pages": 2
  }
}
```

---

## 用户管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/user`

### 1. 获取当前登录用户信息
#### 基本信息
**接口描述**：获取当前登录用户的信息
**请求方式**：GET
**请求路径**：`/info`

#### 请求说明
**请求参数**：无

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "username": "admin",
    "realName": "管理员"
  }
}
```

---

### 2. 创建用户
#### 基本信息
**接口描述**：创建新用户
**请求方式**：POST
**请求路径**：`/`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |
| realName | string | 是 | 真实姓名 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 3. 根据ID查询用户
#### 基本信息
**接口描述**：根据用户ID获取用户信息
**请求方式**：GET
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 用户ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "username": "user001",
    "realName": "张三"
  }
}
```

---

### 4. 获取所有用户列表
#### 基本信息
**接口描述**：获取所有用户列表
**请求方式**：GET
**请求路径**：`/`

#### 请求说明
**请求参数**：无

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "username": "user001",
      "realName": "张三"
    }
  ]
}
```

---

### 5. 修改用户信息
#### 基本信息
**接口描述**：修改用户信息
**请求方式**：PUT
**请求路径**：`/`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 用户ID |
| realName | string | 否 | 真实姓名 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

### 6. 删除用户
#### 基本信息
**接口描述**：删除用户
**请求方式**：DELETE
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 用户ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 7. 分页查询用户列表
#### 基本信息
**接口描述**：分页查询用户列表
**请求方式**：GET
**请求路径**：`/page`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 是 | 页码 |
| pageSize | integer | 是 | 每页数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "username": "user001",
        "realName": "张三"
      }
    ],
    "total": 100,
    "size": 10,
    "current": 1,
    "pages": 10
  }
}
```

---

### 8. 为用户分配角色
#### 基本信息
**接口描述**：为用户分配角色
**请求方式**：PUT
**请求路径**：`/{userId}/roles`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| userId | integer | 是 | 用户ID |

**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| roleIds | integer[] | 是 | 角色ID列表 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

## 系统管理模块

### 角色管理
**当前模块接口请求公共路径前缀**：`/manager/system/role`

#### 1. 分页查询角色列表
#### 基本信息
**接口描述**：分页查询角色列表
**请求方式**：GET
**请求路径**：`/page`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 是 | 页码 |
| pageSize | integer | 是 | 每页数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "管理员"
      }
    ],
    "total": 10,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

---

#### 2. 新增角色
#### 基本信息
**接口描述**：新增角色
**请求方式**：POST
**请求路径**：``

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| name | string | 是 | 角色名称 |
| description | string | 否 | 角色描述 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "name": "编辑"
  }
}
```

---

#### 3. 更新角色
#### 基本信息
**接口描述**：更新角色信息
**请求方式**：PUT
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 角色ID |

**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| name | string | 是 | 角色名称 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "name": "编辑"
  }
}
```

---

#### 4. 删除角色
#### 基本信息
**接口描述**：删除角色
**请求方式**：DELETE
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 角色ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

### 菜单管理
**当前模块接口请求公共路径前缀**：`/manager/system/menu`

#### 1. 获取当前用户的菜单树
#### 基本信息
**接口描述**：获取当前登录用户的菜单树
**请求方式**：GET
**请求路径**：`/user-tree`

#### 请求说明
**请求参数**：无

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 0,
    "name": "root",
    "path": "/",
    "children": []
  }
}
```

---

#### 2. 获取资源树
#### 基本信息
**接口描述**：获取系统的完整资源树
**请求方式**：GET
**请求路径**：`/tree`

#### 请求说明
**请求参数**：无

#### 响应说明
**响应数据示例**：同获取当前用户菜单树的响应结构

---

#### 3. 添加菜单
#### 基本信息
**接口描述**：添加新菜单
**请求方式**：POST
**请求路径**：``

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| name | string | 是 | 菜单名称 |
| path | string | 是 | 菜单路径 |
| parentId | integer | 是 | 父菜单ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

#### 4. 更新菜单
#### 基本信息
**接口描述**：更新菜单信息
**请求方式**：PUT
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 菜单ID |

**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| name | string | 是 | 菜单名称 |
| path | string | 是 | 菜单路径 |
| parentId | integer | 是 | 父菜单ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

#### 5. 删除菜单
#### 基本信息
**接口描述**：删除菜单
**请求方式**：DELETE
**请求路径**：``

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| ids | integer[] | 是 | 菜单ID列表 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

## 单位管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/units`

### 1. 分页查询单位列表
#### 基本信息
**接口描述**：分页查询商品单位列表
**请求方式**：GET
**请求路径**：`/`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 是 | 页码 |
| pageSize | integer | 是 | 每页数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "件",
        "status": true
      }
    ],
    "total": 10,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

---

### 2. 获取单位列表（不分页）
#### 基本信息
**接口描述**：获取所有单位列表，不分页
**请求方式**：GET
**请求路径**：`/list`

#### 请求说明
**请求参数**：无

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "件",
      "status": true
    }
  ]
}
```

---

### 3. 创建单位
#### 基本信息
**接口描述**：创建新单位
**请求方式**：POST
**请求路径**：`/`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| name | string | 是 | 单位名称 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

### 4. 获取单位详情
#### 基本信息
**接口描述**：根据ID获取单位信息
**请求方式**：GET
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 单位ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "name": "件",
    "status": true
  }
}
```

---

### 5. 更新单位信息
#### 基本信息
**接口描述**：更新单位信息
**请求方式**：PUT
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 单位ID |

**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| name | string | 是 | 单位名称 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

### 6. 更新单位状态
#### 基本信息
**接口描述**：更新单位的启用/禁用状态
**请求方式**：PATCH
**请求路径**：`/{id}/status`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 单位ID |

**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| status | boolean | 是 | 单位状态 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

### 7. 删除单位
#### 基本信息
**接口描述**：删除单个单位
**请求方式**：DELETE
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 单位ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

### 8. 批量删除单位
#### 基本信息
**接口描述**：批量删除单位
**请求方式**：DELETE
**请求路径**：`/`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| ids | integer[] | 是 | 单位ID列表 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

## 评论管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/comment`

### 1. 分页查询评论
#### 基本信息
**接口描述**：分页查询商品评论列表
**请求方式**：GET
**请求路径**：`/`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 是 | 页码 |
| pageSize | integer | 是 | 每页数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "goodsName": "苹果手机",
        "content": "商品不错，推荐购买",
        "rating": 5
      }
    ],
    "total": 100,
    "size": 10,
    "current": 1,
    "pages": 10
  }
}
```

---

### 2. 回复评论
#### 基本信息
**接口描述**：对用户评论进行回复
**请求方式**：POST
**请求路径**：`/reply`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| commentId | integer | 是 | 评论ID |
| replyContent | string | 是 | 回复内容 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

## 地址管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/address`

### 1. 分页查询用户地址列表
#### 基本信息
**接口描述**：分页查询用户地址列表
**请求方式**：GET
**请求路径**：`/page`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 否 | 页码，默认1 |
| pageSize | integer | 否 | 每页数量，默认10 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "userId": 10,
        "province": "广东省",
        "city": "深圳市",
        "address": "科技中路1号"
      }
    ],
    "total": 10,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

---

### 2. 查询单个地址
#### 基本信息
**接口描述**：根据ID查询用户地址详情
**请求方式**：GET
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 地址ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "userId": 10,
    "province": "广东省",
    "city": "深圳市",
    "address": "科技中路1号"
  }
}
```

---

### 3. 新增地址
#### 基本信息
**接口描述**：添加新的用户地址
**请求方式**：POST
**请求路径**：`/`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| province | string | 是 | 省份 |
| city | string | 是 | 城市 |
| address | string | 是 | 详细地址 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 4. 修改地址
#### 基本信息
**接口描述**：修改用户地址信息
**请求方式**：PUT
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 地址ID |

**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| province | string | 是 | 省份 |
| city | string | 是 | 城市 |
| address | string | 是 | 详细地址 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 5. 删除地址
#### 基本信息
**接口描述**：删除用户地址
**请求方式**：DELETE
**请求路径**：`/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 地址ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 6. 更新默认地址
#### 基本信息
**接口描述**：设置用户的默认地址
**请求方式**：PUT
**请求路径**：`/{id}/default`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 地址ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

## 公告管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/notice`

### 1. 分页查询公告

#### 基本信息    

**接口描述**：分页查询公告列表  

**请求方式**：GET  
**请求路径**：`/page`  

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| page | integer | 是 | 页码 |
| pageSize | integer | 是 | 每页数量 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "title": "系统维护公告",
        "content": "系统将于明天凌晨2点进行维护",
        "status": true
      }
    ],
    "total": 20,
    "size": 10,
    "current": 1,
    "pages": 2
  }
}
```

---

### 2. 添加公告
#### 基本信息
**接口描述**：添加新公告
**请求方式**：POST
**请求路径**：`/add`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| title | string | 是 | 公告标题 |
| content | string | 是 | 公告内容 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 3. 更新公告
#### 基本信息
**接口描述**：更新公告信息
**请求方式**：POST
**请求路径**：`/update/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 公告ID |

**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| title | string | 是 | 公告标题 |
| content | string | 是 | 公告内容 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 4. 删除单个公告
#### 基本信息
**接口描述**：删除单个公告
**请求方式**：POST
**请求路径**：`/delete/{id}`

#### 请求说明
**路径参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | integer | 是 | 公告ID |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

### 5. 批量删除公告
#### 基本信息
**接口描述**：批量删除公告
**请求方式**：POST
**请求路径**：`/batch/del`

#### 请求说明
**请求头参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| Content-Type | string | 是 | application/json |

**请求体参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| ids | integer[] | 是 | 公告ID列表 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

---

## 统计管理模块
### 模块信息
**当前模块接口请求公共路径前缀**：`/manager/statistics`

### 1. 获取统计数据综合信息
#### 基本信息
**接口描述**：获取统计数据综合信息，包括仪表板概览、收藏概览、销售排行等
**请求方式**：GET
**请求路径**：`/all`

#### 请求说明
**请求参数**：无

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "totalSales": "1000000.00",
    "totalOrders": 5000,
    "todaySales": "5000.00"
  }
}
```

---

### 2. 查询营收趋势
#### 基本信息
**接口描述**：查询指定天数的营收趋势
**请求方式**：GET
**请求路径**：`/revenue-trend`

#### 请求说明
**请求参数**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| days | integer | 否 | 查询天数，默认7天 |

#### 响应说明
**响应数据示例**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "2025-01-01": "5000.00",
    "2025-01-02": "6000.00"
  }
}
```

---

## 附录
### 枚举值定义

#### 订单状态
| 值 | 说明 |
|----|------|
| PENDING | 待支付 |
| PAID | 已支付 |
| SHIPPED | 已发货 |
| DELIVERED | 已收货 |
| CANCELLED | 已取消 |

#### 审核状态
| 值 | 说明 |
|----|------|
| PENDING | 待审核 |
| APPROVED | 已通过 |
| REJECTED | 已拒绝 |

#### 用户角色
| 值 | 说明 |
|----|------|
| ADMIN | 系统管理员 |
| MERCHANT | 商家 |
| USER | 普通用户 |
